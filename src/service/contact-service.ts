import {Contact} from '../model/contact';

export class ContactService {
  addContact = async (contact: Contact) => {
    // TODO
  };

  getContacts = (): Contact[] => {
    // TODO
    return [];
  };
}
